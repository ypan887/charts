
# Override values for pipelines.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

## JFrog Pipelines API server
api:

  ## JFrog Pipelines token to be used between services
  ## For production, the unique token should be generated with: uuidgen | tr '[:upper:]' '[:lower:]'
  token: f562d4fa-cf5f-4d58-8b9b-91ef949c024f

  ## Artifactory password. Recommended is an API key.
  artifactory:
    password: somepassword

  rabbitMq:
    userPassword: somepassword
    uiUserPassword: somepassword


## JFrog Pipelines RabbitMQ server
msg:
  # Do not change this password!!!
  rabbitmqPassword: guest


## PostgreSQL database
postgresql:
  postgresqlPassword: somepassword
